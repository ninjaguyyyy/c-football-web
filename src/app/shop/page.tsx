import { Button } from 'antd';

export default function Shop() {
  return (
    <section className="container mx-auto flex min-h-screen flex-col items-center justify-between bg-white">
      Shop
      <Button type="primary">Ok</Button>
    </section>
  );
}
